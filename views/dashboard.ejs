<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8"><title>Mware Shop</title>
    <style>
        body { background: <%= theme.bg %>; color: <%= theme.text %>; font-family: sans-serif; margin: 0; padding-bottom: 50px; }
        nav { background: #020617; padding: 15px 30px; display: flex; justify-content: space-between; align-items: center; border-bottom: 2px solid <%= theme.accent %>; }
        .logo { font-size: 22px; font-weight: 900; color: #fff; text-decoration: none; }
        .nav-links { display: flex; gap: 20px; align-items: center; }
        .nav-links a { color: #cbd5e1; text-decoration: none; font-size: 14px; font-weight: bold; }
        .badge { background: <%= theme.accent %>; color: #fff; font-size: 11px; padding: 2px 6px; border-radius: 10px; margin-left: 4px; }
        .container { max-width: 1200px; margin: 30px auto; padding: 0 20px; display: grid; grid-template-columns: 3fr 1fr; gap: 30px; }
        .box { background: <%= theme.cardBg %>; border: 1px solid #334155; border-radius: 10px; padding: 20px; margin-bottom: 25px; box-shadow: 0 4px 6px rgba(0,0,0,0.2); }
        .grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); gap: 20px; }
        .prod-card { background: #1e293b; border-radius: 8px; overflow: hidden; border: 1px solid #334155; display: flex; flex-direction: column; justify-content: space-between; }
        .prod-card img { width: 100%; height: 160px; object-fit: cover; }
        .p-body { padding: 15px; flex-grow: 1; display: flex; flex-direction: column; justify-content: space-between; }
        .card-title { font-size: 16px; font-weight: bold; color: white; margin-bottom: 8px; }
        .price { font-family: monospace; font-size: 18px; color: <%= theme.accent %>; font-weight: bold; margin: 5px 0; }
        .status { padding: 2px 6px; font-size: 11px; font-weight: bold; background: rgba(34,197,94,0.2); color: #4ade80; border-radius: 4px; width: max-content; }
        .status.out { background: rgba(239,68,68,0.2); color: #f87171; }
        .btn { width: 100%; padding: 10px; background: <%= theme.btn %>; border: none; color: white; font-weight: bold; border-radius: 6px; cursor: pointer; margin-top: 10px; }
        .qr-img { background: #fff; padding: 6px; border-radius: 6px; width: 130px; height: 130px; margin: 10px auto; display: block; }
        .form-group { margin-bottom: 12px; text-align: left; }
        label { display: block; font-size: 12px; margin-bottom: 4px; color: #94a3b8; font-weight: bold; }
        input, select, textarea { width: 100%; box-sizing: border-box; padding: 10px; background: #0f172a; border: 1px solid #334155; border-radius: 4px; color: white; font-family: inherit; }
        .split-checkout { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }
        @media(max-width: 768px) { .split-checkout { grid-template-columns: 1fr; } .container { grid-template-columns: 1fr; } }
        .summary-row { display: flex; justify-content: space-between; padding: 6px 0; font-size: 14px; border-bottom: 1px dashed #334155; }
        table { width: 100%; border-collapse: collapse; font-size: 13px; text-align: left; margin-top: 15px; }
        th, td { padding: 12px 10px; border-bottom: 1px solid #334155; }
        th { color: #94a3b8; font-weight: bold; background: #0f172a; }
        h2, h3 { margin-top: 0; color: #fff; }
    </style>
</head>
<body>
    <nav>
        <div style="display:flex; align-items:center; gap:10px;">
            <a href="/" class="logo">MWARE SHOP</a>
            <span style="font-size:10px; background:<%= theme.accent %>; color:#000; padding:2px 6px; border-radius:4px; font-weight:bold;"><%= theme.name %></span>
        </div>
        <div class="nav-links">
            <a href="/">Catalog</a>
            <a href="/checkout">Cart <span class="badge"><%= cartCount %></span></a>
            <% if (user) { %>
                <% if (user.role === 'admin') { %><a href="/admin" style="color:#fbbf24;">Admin Panel</a><% } %>
                <a href="/profile" style="color:<%= theme.accent %>;">My Account</a>
                <a href="/logout" style="background:#334155; padding:4px 10px; border-radius:4px;">Logout</a>
            <% } else { %>
                <a href="/login" style="background:<%= theme.btn %>; padding:6px 15px; border-radius:4px;">Login</a>
            <% } %>
        </div>
    </nav>

    <div class="container">
        <div>
            <% if (activeTab === 'shop') { %>
                <h2>Available Inventory</h2>
                <div class="grid">
                    <% products.forEach(p => { %>
                        <div class="prod-card">
                            <img src="<%= p.image %>" alt="<%= p.name %>">
                            <div class="p-body">
                                <div>
                                    <div class="card-title"><%= p.name %></div>
                                    <div class="price"><%= p.price %> <%= p.currency %></div>
                                    <div class="status <%= p.status !== 'In Stock' ? 'out' : '' %>"><%= p.status %></div>
                                </div>
                                <form action="/cart/add" method="POST">
                                    <input type="hidden" name="productId" value="<%= p.id %>">
                                    <button type="submit" class="btn" <%= p.status !== 'In Stock' ? 'disabled' : '' %>>Buy Now</button>
                                </form>
                            </div>
                        </div>
                    <% }) %>
                </div>
            <% } else if (activeTab === 'checkout') { %>
                <h2>Secure Checkout Gateway</h2>
                <% if (cart.length === 0) { %>
                    <div class="box"><p style="margin:0; color:#94a3b8;">Your shopping cart is currently empty.</p></div>
                <% } else { %>
                    <div class="split-checkout">
                        <div class="box">
                            <h3>Shipping Form</h3>
                            <form action="/checkout/pay" method="POST">
                                <input type="hidden" name="totalAmount" value="<%= financials.total %>">
                                <input type="hidden" name="totalCurrency" value="<%= financials.currency %>">
                                <div class="form-group">
                                    <label>Recipient Full Name</label>
                                    <input type="text" name="fullName" required>
                                </div>
                                <div class="form-group">
                                    <label>Shipping Address</label>
                                    <textarea name="shippingAddress" rows="2" required></textarea>
                                </div>
                                <div class="form-group">
                                    <label>Payment Method</label>
                                    <select name="gatewayMethod">
                                        <option value="M-Pesa Express">M-Pesa Mobile Money</option>
                                        <option value="Credit Card">Visa / Mastercard</option>
                                    </select>
                                </div>
                                <button type="submit" class="btn" style="background:#10b981;">Place Order</button>
                            </form>
                        </div>

                        <div class="box" style="display:flex; flex-direction:column; justify-content:space-between;">
                            <div>
                                <h3>Order Details</h3>
                                <div style="max-height:120px; overflow-y:auto;">
                                    <% cart.forEach(c => { %>
                                        <div style="display:flex; justify-content:space-between; margin-bottom:6px; font-size:13px;">
                                            <span style="color:#cbd5e1;"><%= c.name %></span>
                                            <span style="font-family:monospace;"><%= c.price %> <%= financials.currency %></span>
                                        </div>
                                    <% }) %>
                                </div>
                            </div>
                            <div style="border-top:1px solid #334155; padding-top:6px;">
                                <div class="summary-row"><span>Subtotal:</span><span><%= financials.subtotal %></span></div>
                                <div class="summary-row"><span>Shipping:</span><span><%= financials.shipping %></span></div>
                                <div class="summary-row"><span>Tax (16%):</span><span><%= financials.tax %></span></div>
                                <div class="summary-row" style="border:none; font-size:16px; font-weight:bold; color:white; margin-top:6px;">
                                    <span>Total Due:</span><span><%= financials.total %> <%= financials.currency %></span>
                                </div>
                            </div>
                        </div>
                    </div>
                <% } %>
            <% } else if (activeTab === 'profile') { %>
                <h2>Client Self-Service Dashboard</h2>
                
                <div class="box">
                    <h3>My Orders Tracking Hub</h3>
                    <% if (!customerOrders || customerOrders.length === 0) { %>
                        <p style="color:#94a3b8; margin:0;">No orders placed yet.</p>
                    <% } else { %>
                        <div style="overflow-x: auto;">
                            <table>
                                <thead>
                                    <tr>
                                        <th>Order ID</th>
                                        <th>Date</th>
        <aside style="display:flex; flex-direction:column; gap:20px;">
            <div class="box">
                <h4 style="margin:0 0 10px 0; font-size:12px; color:#94a3b8; text-transform:uppercase;">Store Support</h4>
                <div style="font-size:13px; font-family:monospace; line-height:1.6; color:#cbd5e1;">
                    <% if (contactInfo && contactInfo.length > 0) { %>
                        <strong>Ph:</strong> <%= contactInfo[0].phone %><br>
                        <strong>Em:</strong> <%= contactInfo[0].email %><br>
                        <strong>HQ:</strong> <%= contactInfo[0].address %>
                    <% } else if (contactInfo && contactInfo.phone) { %>
                        <strong>Ph:</strong> <%= contactInfo.phone %><br>
                        <strong>Em:</strong> <%= contactInfo.email %><br>
                        <strong>HQ:</strong> <%= contactInfo.address %>
                    <% } else { %>
                        <strong>Ph:</strong> +254 700 000 000<br>
                        <strong>Em:</strong> support@mwareshop.com<br>
                        <strong>HQ:</strong> Mombasa, Kenya
                    <% } %>
                </div>
            </div>
        </aside>
    </div>
</body>
</html>

